services:
  nyayabot-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      NYAYABOT_OLLAMA_URL: http://host.docker.internal:11434
      NYAYABOT_CORS_ORIGINS: http://localhost:5173
    extra_hosts:
      - "host.docker.internal:host-gateway"
